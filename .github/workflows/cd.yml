on:
  push:
    tags:
    - 'release/*'
    
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
     - name: Say hello
       run: echo 'Hello'
     - name: Check Tag
       id: check-tag
       run: echo ${{ github.event.ref }}
